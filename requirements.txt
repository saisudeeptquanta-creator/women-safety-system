# Core
streamlit>=1.36
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
joblib>=1.4
requests>=2.31

# Maps & charts
folium>=0.16
streamlit-folium>=0.21
plotly>=5.22

# Reports
reportlab>=4.2

# Database / cloud (optional — app runs without it via local fallback)
firebase-admin>=6.5

# Helpers
geopy>=2.4
python-dotenv>=1.0
Pillow>=10.3

# Optional: browser GPS capture
# streamlit-geolocation>=0.0.10

# Recommended Python: 3.11 – 3.13. On Python 3.14 (very new) some wheels
# may still be building; if install fails, use a 3.12/3.13 virtualenv.
